import SectionTitle from "../../../Components/SectionTitle";

const Completed = () => {
    return (
        <div className="mt-20">
            <SectionTitle
                headings={'completed'}
                subHeading={'All Completed '}>
            </SectionTitle>
            <div className="px-10 mt-10">
                <h1>job DB</h1>
            </div>
        </div>
    );
};

export default Completed;